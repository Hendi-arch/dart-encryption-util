name: xencryption_util
description: A sample command-line application.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.1.5

# Add regular dependencies here.
dependencies:

  # A Dart library for encryption and decryption.
  pointycastle: ^3.7.4

dev_dependencies:
  lints: ^2.0.0
  test: ^1.24.3
